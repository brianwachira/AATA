import { AdminResolver } from "./AdminResolver";
import { DispatchOfficerResolver } from "./DispatchOfficerResolver";

/**
 * @description holds all resolvers
 */

export const resolvers = [AdminResolver, DispatchOfficerResolver];